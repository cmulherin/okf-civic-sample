# Bundle Update Log

Update history for this bundle (OKF v0.2 §9). Newest first. This log records **edits to the bundle**. A determination *about the organization* is a different thing and lives in [verification.md](verification.md).

## 2026-08-07

* **Addition**: Added `impact.md` (`type: impact`) and `what_i_need_funding_for.md` (`type: funding_need`) — outcomes and results, and funding priorities, both in the organization's own voice. Both are optional enrichment; neither is part of the published `civic/0.6` profile (see each file's own `description`).

## 2026-07-29

* **Update**: Migrated to **OKF v0.2** and **civic/0.6**. `timestamp` replaced by `generated`; provenance claims joined to a frontmatter `sources` list by footnote (§5.1); the simulated determination moved onto core `verified` and `stale_after` (§5.2, §5.5); `index.md` reduced to a listing carrying only `okf_version` (§8); this log reformatted to the §9 date-grouped shape.
* **Update**: Required frontmatter reduced to five `x-civic` keys. Assigned **Candid PCS** codes — Subject `SC030400, SC030407, SJ040700`, Population `PE030000, PG090000, PJ080000`, OrgType `EA000000`. Assigned directly from the activity description; no NTEE to cross from.
* **Update**: `eligibility.md` renamed to `verification.md` (`type: verification`). The eligibility *facts* — organization type and registration country — are now required frontmatter on the organization record; what remains is an optional, dated, third-party determination.
* **Update**: Wikilinks that resolve to a file converted to ordinary markdown links (§6.1). Wikilinks retained only for **emergent terms** with no target file, which the spec explicitly permits and which is how vocabulary the controlled facets cannot carry becomes visible in the graph.

## 2026-07-28

* **Creation**: Bundle created, fabricated for the synthetic collection. Modeled file-for-file on the Chapter 510 Ink bundle. The **second of three international bundles**, carrying a third registry (NIT/RUES/RTE), a third data-protection regime (Ley 1581 de 2012), a third donated-software catalogue, and budget in **COP**, unconverted. Cast around three things the collection could not show otherwise: **digital security as physical security**, since a dataset linking named community monitors to locations and schedules is a targeting package in a country where killings of environmental defenders are documented annually; **community data sovereignty**, where the bundle's most important dataset is owned by eleven separate community organizations with their own governance and the schema has no way to express that; and the collection's most serious note on automated verification — that **adverse-media screening can convert a stigmatization campaign against defenders into a compliance finding**, which turns the verifying system into a delivery vehicle for the attack. Also completes the collection's fourth category of unexaminable data (not-owned, alongside undocumented, absent-by-policy, and privileged). Coalition edges to Detroit and Louisiana make visible that the same monitoring methodology requires incompatible practices in three risk environments. Classification and situation nodes point out to shared `_shared/`.

## 2026-06-18

* **Creation**: Simulated validation determination recorded (APPROVE, 0.89, via NIT/RUES/RTE rather than IRS records; heavy adverse media assessed by human judgement and cleared; monitoring methodology and Ley 1581 compliance out of scope; community-held data not requested). Fabricated workspace `SYNTH-WORKSPACE-0014`.
