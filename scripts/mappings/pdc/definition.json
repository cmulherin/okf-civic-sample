{
  "changemaker": {
    "name": "{{organization_name}}"
  },
  "fields": [
    {
      "baseFieldShortCode": "{{code}}",
      "type": "string",
      "value": "{{value}}",
      "source": "{{source}}"
    }
  ]
}
